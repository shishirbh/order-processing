# General Flow — Order Processing

_This is a placeholder / dummy file. Replace with real content as the General Flow knowledge grows._

This document is the source of truth for **non-vendor-specific** order-processing
questions. When the user is in the **General** tab, the assistant must answer
only from this file and ignore the per-vendor knowledge base.

---

## 1. Scope

The General Flow covers cross-cutting topics that apply regardless of vendor:

- General order intake from sales reps
- Customer-facing communication templates (acknowledgment, ETA updates)
- Pricing-policy basics that aren't vendor-specific
- Internal escalation paths (who to ping when X happens)
- Holiday / blackout calendars
- Default payment terms when a vendor doesn't override them

---

## 2. Placeholder content

> **TODO:** Fill in the real general-flow SOP here.
>
> Until this file is populated, the assistant should respond honestly: "The
> general flow knowledge base is currently a placeholder — please ask a more
> specific question or switch to the Vendors tab."

---

## 3. What this file is NOT

- Not a vendor directory — use `Vendor Information.md` or `Vendors/<Vendor>/`
- Not a claims playbook — use `Issue resolution.md` or the vendor's notes
- Not a sales SOP — those live in `_shared_sops/`

---

## 4. Output contract (when answering from this file)

- Lead with the direct answer.
- Quote verbatim where exact wording matters.
- If this file doesn't cover the question, say so explicitly — do not fall
  back to vendor data.
- Always end with a `Sources:` section citing `general_flow.md`.
