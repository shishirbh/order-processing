# --- Pre-cleanup leftovers (cannot delete while Excel holds the lock) ---
orignal files/

# --- Archived Office originals (not canonical; .md in vendor folders is source of truth) ---
_source_docx/

# --- Office lockfiles / temp files ---
~$*
*.tmp
.~lock.*

# --- OS metadata ---
Thumbs.db
Desktop.ini
.DS_Store

# --- Editor / IDE ---
.vscode/
.idea/
*.swp
*.swo

# --- Skill eval artifacts ---
# The review HTML pages are committed so the team can see how each skill was tested.
# Regenerated artifacts (feedback.json downloads, scratch files) should stay local:
_skill_evals/feedback*.json
_skill_evals/*.tmp
